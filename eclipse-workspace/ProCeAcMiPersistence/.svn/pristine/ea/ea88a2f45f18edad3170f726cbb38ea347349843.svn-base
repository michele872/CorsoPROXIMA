drop table surveytag;
create table surveytag(
	surveyid int not null,
	surveycategoryid int not null,
	FOREIGN KEY (surveyid) REFERENCES survey (id),
	FOREIGN KEY (surveycategoryid) REFERENCES surveycategory (id)
);

SELECT * FROM surveytag ;